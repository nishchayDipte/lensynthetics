# LendSynthetix v2.0 — Dependencies
# Install: pip install -r requirements.txt

# ── Core ───────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.18.0
python-dotenv>=1.0.0
pydantic>=2.0.0

# ── LangGraph + LangChain ──────────────────────────────────
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-groq>=0.1.0

# ── Google Gemini (new SDK — not deprecated) ───────────────
langchain-google-genai>=2.0.0
google-genai>=0.8.0

# ── LlamaIndex ─────────────────────────────────────────────
llama-index>=0.10.0
llama-index-llms-groq>=0.1.0
llama-index-embeddings-langchain>=0.1.0
llama-index-readers-file>=0.1.0

# ── PDF Parsing ────────────────────────────────────────────
pdfplumber>=0.10.0
PyPDF2>=3.0.0
pypdf>=4.0.0

# ── Vector Store ───────────────────────────────────────────
faiss-cpu>=1.7.4

# ── Fallback embeddings (no API key needed) ────────────────
# Uncomment if you don't have a Google API key:
# llama-index-embeddings-huggingface>=0.2.0
# sentence-transformers>=2.7.0